<div class="box span12">
  <div class="box-content">
    <h2>List Items</h2>
    <table id="items-table" class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>Nama Barang</th>
          <th>Harga</th>
          <th>qty</th>
          <th>Subtotal</th>
        </tr>
      </thead>
      <tbody>
        <%= f.fields_for :purchase_order_details do |detail| %>
          <tr class="fields">
            <%= f.fields_for :items do |item| %>
              <td>
                <%= item.text_field :name, readonly: true %>
                <%= detail.hidden_field :item_id, readonly: true %>
              </td>
            <% end %>
            <td><%= detail.text_field :purchase_price, readonly: true %></td>
            <td>
              <%= detail.check_box :is_ppn %>
            </td>
            <td>
              <%= link_to_remove_fields('<i class="icon-remove"></i>'.html_safe, detail) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>